{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "dev:web": "pnpm --filter @workspace/themahj run dev",
    "dev:api": "pnpm --filter @workspace/api-server run dev"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
